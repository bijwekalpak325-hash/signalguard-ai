'use client';
import { FileText, Download } from 'lucide-react';
export default function ReportsPage() { const download = (format: string) => { window.location.href = `/api/reports?format=${format}`; }; return <div className="space-y-6"><div><h1 className="text-3xl font-bold text-white mb-2">Reports</h1><p className="text-slate-400">Export actual processing sessions, evidence, and recommendations</p></div><section className="bg-slate-800 border border-slate-700 rounded-xl p-6"><h2 className="text-lg font-semibold text-white mb-4 flex items-center gap-2"><FileText className="w-5 h-5 text-cyan-400" />Generate Report</h2><p className="text-slate-400 text-sm mb-4">Reports include all locally stored sessions for the signed-in operator.</p><div className="flex gap-3"><button onClick={() => download('csv')} className="flex-1 px-4 py-3 bg-slate-700 text-white rounded-lg hover:bg-slate-600 flex items-center justify-center gap-2"><Download className="w-4 h-4" />CSV</button><button onClick={() => download('json')} className="flex-1 px-4 py-3 bg-cyan-500 text-white rounded-lg hover:bg-cyan-600 flex items-center justify-center gap-2"><Download className="w-4 h-4" />JSON</button></div></section></div> }
